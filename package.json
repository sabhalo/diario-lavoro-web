{
  "name": "diario-di-lavoro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:browser-asr": "node scripts/build-browser-asr.mjs",
    "smoke:browser": "node scripts/run-browser-smokes.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright-core": "^1.63.0"
  }
}
