import { canTranscribe, captureIsLive, CHUNK_MS, DiaryStore, closeRecording, exportableChunks, exportTranscript, formatTime, gapAfterConfirmed, id, isoNow, mediaFileName, nextRecording, overlapsScope, recordingExportScope, safeFileName, searchDocuments, sessionOffset, storageAdmission, supersededAsrSegmentsForBlocks, transcriptText } from "./core.js?v=9";
import { createZip, streamZip } from "./zip.js?v=2";
import { ASR_PROFILES, asrChunks, asrProfile, asrResultShape, audioMetrics, createAsrPipeline, decodeTo16k, usesWholeBlockTimestamp, webGpuAvailable } from "./asr-core.js?v=6";
const state = { store: null, view: "home", selectedId: null, jumpOffset: null, display: null, mic: null, displayInfo: null, tests: { system: null, mic: null }, meters: new Map(), recording: null, segmenters: [], capturing: false, stopping: false, asr: { status: "in attesa" }, asrPipeline: null, asrModule: null };
const view = document.querySelector("#view"), dialog = document.querySelector("#dialog"), storageStatus = document.querySelector("#storage-status");
const esc = (value = "") => String(value).replace(/[&<>"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" })[char]);
const byId = (id) => document.getElementById(id);
const localDate = (iso) => iso ? new Intl.DateTimeFormat("it-IT", { dateStyle: "medium", timeStyle: "short" }).format(new Date(iso)) : "—";
const streamState = (stream) => stream?.getTracks().some((track) => track.readyState === "live") ? "live" : "assente";
const persistedAsrProfile = () => asrProfile(state.asr.profileId || (state.asr.model === "Xenova/whisper-small" ? "rapid" : state.asr.model) || "rapid");
const selectedAsrProfile = () => asrProfile(byId("asr-model")?.value || persistedAsrProfile().id);
const asrOptions = () => Object.values(ASR_PROFILES).map((profile) => `<option value="${profile.id}" ${persistedAsrProfile().id === profile.id ? "selected" : ""}>${esc(profile.label)}: ${esc(profile.model.replace("Xenova/whisper-", "Whisper "))} ${profile.dtype}${profile.device === "webgpu" ? " · WebGPU" : ""} — ${esc(profile.size)}</option>`).join("");

async function init() {
  if (!window.isSecureContext) showNotice("Questa app richiede localhost o HTTPS per chiedere le catture.", "danger");
  try { state.store = await new DiaryStore().open(); state.asr = (await state.store.get("settings", "asr-settings")) || state.asr; await updateStorage(); await recoverInterrupted(); await render(); }
  catch (error) { view.innerHTML = `<div class="callout danger"><strong>Archivio locale non disponibile.</strong><br>${esc(error.message)}</div>`; }
  document.addEventListener("click", onClick); document.addEventListener("submit", onSubmit); document.addEventListener("change", onChange);
  navigator.serviceWorker?.register("sw.js").catch(() => {});
}

async function updateStorage() {
  if (!navigator.storage?.estimate) { storageStatus.textContent = "Archivio locale nel browser"; return; }
  const { usage = 0, quota = 0 } = await navigator.storage.estimate();
  storageStatus.textContent = `Archivio locale: ${(usage / 1024 / 1024).toFixed(1)} MB${quota ? ` di ${(quota / 1024 / 1024).toFixed(0)} MB` : ""}`;
}

async function recoverInterrupted() {
  const sessions = await state.store.all("sessions");
  for (const session of sessions.filter((item) => item.state === "aperta")) {
    const recordings = await state.store.bySession("recordings", session.id);
    for (const recording of recordings.filter((item) => item.status === "in-corso")) {
      const chunks = await state.store.byRecording("chunks", recording.id);
      for (const chunk of chunks.filter((item) => item.status === "scritto" || item.status === "confermato")) {
        const playable = !!chunk.blob && await isPlayable(chunk.blob, chunk.stream);
        if (!playable) {
          chunk.status = "non verificabile"; chunk.verifiedAt = isoNow(); await state.store.put("chunks", chunk);
          await state.store.put("gaps", { id: id("gap"), sessionId: session.id, recordingId: recording.id, startMs: chunk.startMs, endMs: chunk.endMs, cause: "blocco non recuperabile alla riapertura", certainty: "misurata", createdAt: isoNow() });
        } else if (chunk.status === "scritto") { chunk.status = "confermato"; chunk.verifiedAt = isoNow(); await state.store.put("chunks", chunk); }
      }
      const closed = closeRecording(recording, session, "interrotta", "riapertura dopo chiusura o crash");
      await state.store.put("recordings", closed);
      const gap = gapAfterConfirmed(closed, chunks, "intervallo dopo chiusura non verificabile", "incerta");
      if (gap) await state.store.put("gaps", { id: id("gap"), ...gap, createdAt: isoNow() });
      session.state = "interrotta/in attesa di scelta"; session.updatedAt = isoNow(); await state.store.put("sessions", session);
    }
  }
}

function showNotice(text, tone = "") { const current = document.querySelector("#notice"); if (current) current.remove(); view.insertAdjacentHTML("afterbegin", `<div id="notice" class="callout ${tone}">${esc(text)}</div>`); }
function setView(name) { state.view = name; document.querySelectorAll(".nav").forEach((button) => button.classList.toggle("active", button.dataset.view === name)); render(); }

async function render() {
  if (!state.store) return;
  if (state.view === "home") return renderHome();
  if (state.view === "capture") return renderCapture();
  if (state.view === "search") return renderSearch();
  return renderVerification();
}

async function renderHome() {
  const sessions = (await state.store.all("sessions")).sort((a, b) => b.updatedAt.localeCompare(a.updatedAt));
  if (!sessions.length) { view.innerHTML = document.querySelector("#empty-template").innerHTML; return; }
  view.innerHTML = `<header><div><h1>Sessioni</h1><p class="muted">Episodi di lavoro locali, ordinati dall’ultimo aggiornamento.</p></div><button data-action="new-session">Nuova sessione</button></header><div class="session-list">${sessions.map((session) => `<article class="session-row" data-action="open-session" data-id="${session.id}"><div class="grow"><strong>${esc(session.title)}</strong><p class="small muted">${localDate(session.updatedAt)} · ${session.timezone}</p></div><span class="status ${session.state === "conclusa" ? "ok" : "warn"}">${esc(session.state)}</span></article>`).join("")}</div>`;
}

async function selectedSession() { return state.selectedId ? state.store.get("sessions", state.selectedId) : null; }
async function renderCapture() {
  const session = await selectedSession();
  if (!session) { view.innerHTML = `<div class="empty"><h2>Scegli una sessione</h2><p>La cattura appartiene sempre a una sessione nominabile.</p><button data-action="new-session">Crea sessione</button></div>`; return; }
  const recordings = await state.store.bySession("recordings", session.id), gaps = await state.store.bySession("gaps", session.id), notes = await state.store.bySession("notes", session.id), events = await state.store.bySession("events", session.id), transcripts = await state.store.bySession("transcripts", session.id);
  const allChunks = (await state.store.all("chunks")).filter((chunk) => chunk.sessionId === session.id).sort((a, b) => a.startMs - b.startMs);
  const active = state.recording?.sessionId === session.id;
  const hasDisplayAudio = !!state.display?.getAudioTracks().length;
  const displaySurface = state.displayInfo?.surface || "non selezionato";
  const completeReady = captureIsLive({ displaySurface, displayTracks: state.display?.getTracks().map((track) => track.readyState) || [], microphoneTracks: state.mic?.getTracks().map((track) => track.readyState) || [], systemTest: state.tests.system, microphoneTest: state.tests.mic });
  const items = [...recordings.map((r) => ({ type: "Registrazione", at: r.offsetStartMs, text: `${r.mode} · ${r.status}${r.cause ? ` (${r.cause})` : ""}`, gap: r.status === "interrotta" })), ...gaps.map((g) => ({ type: "Lacuna", at: g.startMs, text: g.cause, gap: true })), ...events.map((e) => ({ type: "Evento", at: e.startMs ?? 0, text: e.text })), ...notes.map((n) => ({ type: "Nota", at: n.startMs ?? 0, text: n.text }))].sort((a, b) => a.at - b.at);
  view.innerHTML = `<header><div><h1>${esc(session.title)}</h1><p class="muted">${esc(session.state)} · inizio ${localDate(session.startedAt)}</p></div><div class="actions"><button class="secondary compact" data-action="rename-session">Rinomina</button><button class="secondary compact" data-action="export-session">Esporta</button><button class="secondary compact" data-action="delete-session">Rimuovi</button></div></header>
  ${session.attestation ? `<div class="callout">Attestazione resa il ${localDate(session.attestation.at)}. Non certifica policy o consenso di altre persone.</div>` : `<div class="callout warn"><strong>Attestazione richiesta.</strong> Prima di una cattura, dichiara di aver verificato gli obblighi applicabili.</div>`}
  <div class="grid"><section class="card"><h2>Preflight dei flussi</h2><p class="muted">Scegli il monitor e l’audio del computer in Chrome; il microfono è richiesto separatamente.</p>
  ${streamRow("Monitor + audio computer", state.display, state.displayInfo ? `${displaySurface}; audio ${hasDisplayAudio ? "presente" : "assente"}` : "non richiesto", "request-display", active)}
  ${streamRow("Microfono", state.mic, streamState(state.mic), "request-mic", active)}
  <div class="actions"><button class="secondary compact" data-action="sample-system" ${!state.display || !hasDisplayAudio || active ? "disabled" : ""}>Prova audio computer</button><button class="secondary compact" data-action="sample-mic" ${!state.mic || active ? "disabled" : ""}>Prova microfono</button></div>
  ${testRow("Sistema", state.tests.system)}${testRow("Microfono", state.tests.mic)}
  <label class="small"><input id="reduced-optin" type="checkbox" ${completeReady ? "" : ""}> Accetto esplicitamente una registrazione <strong>ridotta</strong> se la prova completa non è pronta.</label>
  <div class="actions">${!session.attestation ? `<button data-action="attest">Rendi attestazione</button>` : active ? `<button class="danger" data-action="pause">Pausa / ferma tratto</button><button class="secondary" data-action="conclude">Concludi sessione</button>` : `<button data-action="start-capture" ${session.state === "conclusa" ? "disabled" : ""}>Avvia cattura</button><button class="secondary" data-action="conclude" ${session.state === "conclusa" ? "disabled" : ""}>Concludi sessione</button>`}</div>
  ${active ? `<p class="small"><span class="status ok">in corso</span> Salvataggio a blocchi autonomi di ${CHUNK_MS / 1000}s; i blocchi diventano confermati solo dopo verifica di riproducibilità.</p>` : `<p class="small muted">${completeReady ? "Modalità completa pronta." : "La modalità completa richiede monitor, due tracce audio e i due riascolti positivi."}</p>`}</section>
  <section class="card"><h2>Stato e recupero</h2><div id="capture-status">${renderCaptureStatus(recordings)}</div>${recordings.length ? `<div class="session-list">${recordings.sort((a, b) => b.offsetStartMs - a.offsetStartMs).map((recording) => `<div class="split small"><span>${formatTime(recording.offsetStartMs)} · ${esc(recording.mode)} · ${esc(recording.status)}</span><button class="secondary compact" data-action="export-recording" data-id="${recording.id}">Esporta tratto</button></div>`).join("")}</div>` : ""}<p class="small muted">Una chiusura o perdita di flusso crea un’interruzione e non viene mai rappresentata come contenuto acquisito.</p></section>
  <section class="card"><h2>Aggiungi contesto</h2><form data-form="note"><label>Nota libera<textarea name="text" required placeholder="Riflessione o contesto, non un segmento ASR"></textarea></label><button>Salva nota</button></form><form data-form="event"><label>Evento fattuale<textarea name="text" required placeholder="Ad esempio: decisione presa"></textarea></label><button class="secondary">Aggiungi evento</button></form></section>
  <section class="card"><h2>Trascrizione locale</h2><p id="asr-status">${renderAsrStatus(session)}</p><p class="small muted">Su gesto esplicito scarica Transformers.js 3.8.1 da jsDelivr e un modello pubblico da Hugging Face. Audio e testo non vengono caricati: l’inferenza avviene nel browser. Alta qualità usa Whisper Small q8/WASM (circa 252 MB di pesi più runtime/cache): è stato scelto perché il precedente Small fp16/WebGPU ha prodotto output vuoto nel loop sintetico e non viene più proposto. Se il modello restituisce testo senza timestamp, il testo viene salvato sull’intera durata del blocco e la strategia resta registrata. Dopo la cache, disconnetti la rete e usa “Verifica cache offline”.</p><div class="actions"><select id="asr-model">${asrOptions()}</select><button class="secondary compact" data-action="prepare-asr" ${state.asr.status === "download in corso" ? "disabled" : ""}>Scarica e prepara ASR locale</button><select id="asr-source"><option value="microfono">Blocchi microfono confermati</option><option value="display">Blocchi audio computer confermati</option></select><button class="secondary compact" data-action="transcribe" ${canTranscribe({ pipelineReady: !!state.asrPipeline, preparedModel: state.asr.model, selectedModel: persistedAsrProfile().model }) ? "" : "disabled"}>Trascrivi blocchi confermati</button><button class="secondary compact" data-action="verify-asr-offline" ${state.asr.status !== "pronto" ? "disabled" : ""}>Verifica cache offline</button></div>${Object.entries(session.asrCoverage || {}).map(([kind, coverage]) => `<p class="small"><strong>Diagnostica ${esc(kind)}:</strong> ${coverage.diagnostics?.map((item) => `${formatTime(item.startMs)} ${item.seconds.toFixed(2)}s RMS ${item.rms.toFixed(4)} peak ${item.peak.toFixed(4)} ${esc(item.format || "")} ${esc(item.model || "")} ${esc(item.profileId || "")} out=${item.output?.textChars ?? "?"}/${item.output?.chunks ?? "?"}/${item.output?.nonEmptyChunks ?? "?"} ${(item.elapsedMs || 0).toFixed(0)}ms${item.timestampStrategy ? ` ${esc(item.timestampStrategy)}` : ""}${item.error ? ` errore=${esc(item.error)}` : ""}`).join(" · ") || "nessuna misura"}${coverage.failed ? ` · ${coverage.failed} blocchi non trascritti` : ""}</p>`).join("")}${transcripts.filter((item) => item.source === "asr-locale" && item.active !== false).length ? `<div class="session-list">${transcripts.filter((item) => item.source === "asr-locale" && item.active !== false).map((item) => `<div class="split small"><span>${formatTime(item.startMs)} · ${esc(item.text)}</span><button class="secondary compact" data-action="edit-segment" data-id="${item.id}">Correggi</button></div>`).join("")}</div>` : ""}<form data-form="transcript"><label>Segmento temporizzato (testo riconosciuto o correzione)<textarea name="text" required placeholder="Testo"></textarea></label><div class="grid"><label>Inizio (secondi)<input name="start" type="number" min="0" step="0.1" required></label><label>Fine (secondi)<input name="end" type="number" min="0" step="0.1" required></label></div><label>Provenienza<select name="source"><option value="manuale-locale">correzione manuale locale</option><option value="asr-locale-importato">ASR locale importato</option></select></label><button class="secondary">Salva segmento</button></form></section>
  <section class="card wide"><h2>Riproduzione dei blocchi</h2>${state.jumpOffset != null ? `<p class="callout">Punto richiesto: ${formatTime(state.jumpOffset)}. Il blocco evidenziato contiene il timestamp cercato.</p>` : ""}${allChunks.length ? `<div class="session-list">${allChunks.map((chunk) => { const focus = state.jumpOffset != null && chunk.startMs <= state.jumpOffset && chunk.endMs >= state.jumpOffset; return `<div class="split ${focus ? "focus" : ""}"><div><span class="status ${chunk.status === "confermato" ? "ok" : "warn"}">${esc(chunk.status)}</span> <strong>${esc(chunk.stream)}</strong> <span class="small muted">${formatTime(chunk.startMs)}–${formatTime(chunk.endMs)} · ${(chunk.bytes / 1024 / 1024).toFixed(2)} MB</span></div><button class="secondary compact" data-action="play-block" data-id="${chunk.id}">Riproduci</button></div>`; }).join("")}</div>` : `<p class="muted">I blocchi salvati appariranno qui. Solo quelli confermati sono dichiarati riproducibili.</p>`}</section>
  <section class="card wide"><h2>Timeline della sessione</h2>${items.length ? `<div class="timeline">${items.map((item) => `<div class="timeline-item ${item.gap ? "gap" : ""}"><span class="chip">${formatTime(item.at)}</span><strong> ${item.type}</strong><p>${esc(item.text)}</p></div>`).join("")}</div>` : `<p class="muted">Ancora nessun tratto, nota o evento.</p>`}</section></div>`;
  refreshMeters();
  if (state.jumpOffset != null) requestAnimationFrame(() => document.querySelector(".focus")?.scrollIntoView({ block: "center", behavior: "smooth" }));
}
function streamRow(label, stream, detail, action, disabled = false) { const live = streamState(stream) === "live"; return `<div class="stream"><i class="dot ${live ? "live" : ""}"></i><div><strong>${label}</strong><div class="small muted">${esc(detail)}</div><div class="meter"><i id="meter-${action}"></i></div></div><button class="secondary compact" data-action="${action}" ${disabled ? "disabled" : ""}>${live ? "Rifai" : "Richiedi"}</button></div>`; }
function testRow(label, test) { return `<p class="small"><span class="status ${test?.passed ? "ok" : test ? "warn" : ""}">${label}: ${test?.passed ? "riascolto confermato" : test?.url ? "campione pronto" : "non verificato"}</span>${test?.url ? ` <button class="secondary compact" data-action="play-test" data-kind="${label === "Sistema" ? "system" : "mic"}">Riascolta</button><button class="compact" data-action="confirm-test" data-kind="${label === "Sistema" ? "system" : "mic"}">Segna riconoscibile</button>` : ""}</p>`; }
function renderCaptureStatus(recordings) { const latest = recordings.sort((a, b) => b.updatedAt.localeCompare(a.updatedAt))[0]; return latest ? `<p><span class="status ${latest.status === "interrotta" ? "fail" : latest.status === "terminata" ? "ok" : "warn"}">${esc(latest.status)}</span></p><p class="small">${esc(latest.cause || "Nessuna causa di arresto")}</p>` : `<p class="muted">Nessun tratto salvato.</p>`; }
function renderAsrStatus(session) { const detail = state.asr.detail ? ` ${esc(state.asr.detail)}` : ""; return `Stato sessione: <strong>${esc(session.asrStatus || "in attesa")}</strong>. Motore: <strong>${esc(state.asr.status || "in attesa")}</strong>.${detail}`; }

async function renderSearch() {
  const sessions = (await state.store.all("sessions")).sort((a, b) => a.title.localeCompare(b.title));
  view.innerHTML = `<header><div><h1>Ricerca</h1><p class="muted">Titoli, note, eventi e trascrizioni locali. La ricerca predefinita usa il testo attivo.</p></div></header><section class="card"><form data-form="search"><label>Termine<input name="query" autofocus required placeholder="Cerca nella cronologia"></label><div class="grid"><label>Da<input name="from" type="date"></label><label>A<input name="to" type="date"></label><label>Sessione<select name="sessionId"><option value="">Tutte le sessioni</option>${sessions.map((session) => `<option value="${esc(session.id)}">${esc(session.title)}</option>`).join("")}</select></label></div><label class="small"><input name="originalAsr" type="checkbox"> Cerca anche originali ASR e versioni sostituite</label><button>Cerca</button></form><div id="results" class="session-list"></div></section>`;
}
async function renderVerification() {
  view.innerHTML = `<header><div><h1>Verifica e limiti</h1><p class="muted">Lo sviluppo locale non sostituisce le prove sulla build Chrome/macOS.</p></div></header><section class="card"><h2>Stato onesto della consegna</h2><ul class="checklist"><li><strong>AC1–AC2, preflight dei tre flussi:</strong> esito breve positivo solo riferito dall’utente; non osservato dall’agente né misurato sul profilo target.</li><li><strong>AC7, ASR italiano:</strong> esito positivo solo riferito dall’utente; profilo/modello, metriche, versione Chrome e benchmark non sono disponibili.</li><li><strong>AC3–AC6 e AC8–AC10:</strong> ancora da provare sulla build e sul Mac target, incluse due ore, offline, quota, crash/revoca/sleep e riapertura export.</li><li><strong>Uso reale con dati aziendali o persone:</strong> bloccato dal gate policy, ancora senza evidenza aziendale.</li></ul><p>Il preflight controlla selezione monitor e presenza delle tracce; un indicatore live non è prova di contenuto. La prova guidata conserva solo il suo esito, non i campioni.</p><p><a href="docs/verification/capture-preflight-mac-reported-2026-09-22.md" target="_blank">Resoconto della prova breve riferita</a> · <a href="docs/verification/acceptance-matrix-2026-09-22.md" target="_blank">Matrice dei criteri di accettazione</a></p></section>`;
}

async function onClick(event) {
  const button = event.target.closest("[data-action],.nav"); if (!button) return;
  if (button.classList.contains("nav")) return setView(button.dataset.view);
  const action = button.dataset.action;
  if (action === "new-session") return openNewSession();
  if (action === "create-session") return createSession(dialog.querySelector("[name=title]")?.value);
  if (action === "rename-session") return openRenameSession();
  if (action === "edit-segment") return openCorrection(button.dataset.id);
  if (action === "jump-to") { state.selectedId = button.dataset.id; state.jumpOffset = Number(button.dataset.offset); return setView("capture"); }
  if (action === "open-session") { state.selectedId = button.dataset.id; state.jumpOffset = button.dataset.offset ? Number(button.dataset.offset) : null; return setView("capture"); }
  if (action === "attest") return openAttestation();
  if (action === "request-display") return acquireDisplay();
  if (action === "request-mic") return acquireMic();
  if (action === "sample-system") return sample("system");
  if (action === "sample-mic") return sample("mic");
  if (action === "prepare-asr") return prepareAsr().catch(() => {}).finally(() => renderCapture());
  if (action === "transcribe") return transcribeSession();
  if (action === "verify-asr-offline") return verifyAsrOffline();
  if (action === "play-test") return playTest(button.dataset.kind);
  if (action === "confirm-test") return confirmTest(button.dataset.kind);
  if (action === "start-capture") return startCapture();
  if (action === "pause") return stopCapture("terminata", "pausa richiesta");
  if (action === "conclude") return concludeSession();
  if (action === "export-session") return exportSession();
  if (action === "export-recording") return exportScope(button.dataset.id);
  if (action === "play-block") return playBlock(button.dataset.id);
  if (action === "delete-session") return confirmDelete();
}
async function onChange(event) { if (event.target.id !== "asr-model") return; const profile = asrProfile(event.target.value); state.asrPipeline = null; await setAsr("da preparare", "Profilo cambiato: usa “Scarica e prepara ASR locale” prima di trascrivere.", { model: profile.model, profileId: profile.id, dtype: profile.dtype, device: profile.device }); renderCapture(); }

async function onSubmit(event) {
  const form = event.target; if (!form.dataset.form) return; event.preventDefault();
  const data = new FormData(form);
  if (form.dataset.form === "new-session") return createSession(data.get("title"));
  const session = await selectedSession(); if (!session) return;
  if (form.dataset.form === "rename-session") { const title = data.get("title")?.trim(); if (!title) return showNotice("Il titolo non può essere vuoto.", "danger"); session.title = title; session.updatedAt = isoNow(); await state.store.put("sessions", session); dialog.close(); return renderCapture(); }
  if (form.dataset.form === "correct-segment") { const original = await state.store.get("transcripts", data.get("segmentId")); if (!original) return showNotice("Segmento originale non trovato.", "danger"); original.active = false; await state.store.put("transcripts", original); await state.store.put("transcripts", { ...original, id: id("correction"), text: data.get("text").trim(), source: "manuale-locale", engine: "persona", parentId: original.id, version: (original.version || 1) + 1, active: true, createdAt: isoNow() }); dialog.close(); return renderCapture(); }
  if (form.dataset.form === "attest") { session.attestation = { at: isoNow(), textVersion: "v1", declaration: "Ho verificato gli obblighi applicabili prima della cattura." }; session.updatedAt = isoNow(); await state.store.put("sessions", session); dialog.close(); return renderCapture(); }
  if (form.dataset.form === "note" || form.dataset.form === "event") { const at = sessionOffset(session.startedAt); await state.store.put(form.dataset.form === "note" ? "notes" : "events", { id: id(form.dataset.form), sessionId: session.id, recordingId: state.recording?.id ?? null, text: data.get("text").trim(), startMs: at, createdAt: isoNow(), kind: form.dataset.form }); form.reset(); return renderCapture(); }
  if (form.dataset.form === "transcript") { const startMs = Number(data.get("start")) * 1000, endMs = Number(data.get("end")) * 1000, source = data.get("source"); if (endMs < startMs) return showNotice("La fine del segmento deve seguire l’inizio.", "danger"); const originals = (await state.store.bySession("transcripts", session.id)).filter((item) => item.source === "asr-locale" && item.active !== false && item.startMs <= startMs && item.endMs >= endMs).sort((a, b) => a.createdAt.localeCompare(b.createdAt)); const original = source === "manuale-locale" ? originals.at(-1) : null; if (original) { original.active = false; await state.store.put("transcripts", original); } await state.store.put("transcripts", { id: id("asr"), sessionId: session.id, recordingId: original?.recordingId ?? state.recording?.id ?? null, startMs, endMs, text: data.get("text").trim(), source, engine: source === "manuale-locale" ? "persona" : "ASR locale importato", parentId: original?.id ?? null, version: (original?.version || 0) + 1, active: true, createdAt: isoNow() }); session.asrStatus = "parziale"; session.updatedAt = isoNow(); await state.store.put("sessions", session); form.reset(); return renderCapture(); }
  if (form.dataset.form === "search") return runSearch(data);
  if (form.dataset.form === "delete") { try { const result = await state.store.deleteSession(session.id); dialog.close(); state.selectedId = null; await updateStorage(); setView("home"); showNotice(`Sessione rimossa: ${result.recordings} tratti e ${result.chunks} blocchi rimossi dall’archivio controllato dall’app. Esportazioni o backup esterni non sono controllati dall’app.`, "warn"); } catch (error) { showNotice(`Rimozione non completata (${error.name || "errore"}): la sessione non è dichiarata rimossa. Riprova o conserva l’errore per verifica.`, "danger"); } }
}

function openNewSession() { dialog.innerHTML = `<div class="dialog-body"><h2>Nuova sessione</h2><form data-form="new-session"><label>Titolo della sessione<input name="title" required maxlength="120" placeholder="Es. Preparazione presentazione"></label><p class="small muted">Può esistere senza cattura.</p><div class="actions"><button type="button" data-action="create-session">Crea</button><button class="secondary" type="button" onclick="this.closest('dialog').close()">Annulla</button></div></form></div>`; dialog.showModal(); }
async function openRenameSession() { const session = await selectedSession(); dialog.innerHTML = `<div class="dialog-body"><h2>Rinomina sessione</h2><form data-form="rename-session"><label>Titolo<input name="title" maxlength="120" required value="${esc(session.title)}"></label><div class="actions"><button>Salva</button><button class="secondary" type="button" onclick="this.closest('dialog').close()">Annulla</button></div></form></div>`; dialog.showModal(); }
async function openCorrection(segmentId) { const segment = await state.store.get("transcripts", segmentId); if (!segment) return showNotice("Segmento non trovato.", "danger"); dialog.innerHTML = `<div class="dialog-body"><h2>Correggi segmento</h2><p class="small muted">Originale ASR conservato separatamente; questa versione diventerà quella attiva.</p><form data-form="correct-segment"><input type="hidden" name="segmentId" value="${segment.id}"><label>Testo<input name="text" required value="${esc(segment.text)}"></label><div class="actions"><button>Salva correzione</button><button class="secondary" type="button" onclick="this.closest('dialog').close()">Annulla</button></div></form></div>`; dialog.showModal(); }
async function createSession(title) { const clean = title?.trim(); if (!clean) return showNotice("Inserisci un titolo per la sessione.", "danger"); const now = isoNow(); const item = { id: id("ses"), title: clean, state: "aperta", startedAt: now, timezone: Intl.DateTimeFormat().resolvedOptions().timeZone, createdAt: now, updatedAt: now, asrStatus: "in attesa" }; await state.store.put("sessions", item); state.selectedId = item.id; dialog.close(); setView("capture"); }
function openAttestation() { dialog.innerHTML = `<div class="dialog-body"><h2>Attestazione di registrazione</h2><p>Dichiari di aver verificato le regole applicabili e assolto gli obblighi preliminari richiesti per questa sessione con cattura. Non equivale a un’autorizzazione aziendale né prova il consenso di altre persone.</p><form data-form="attest"><label><input type="checkbox" required> Confermo questa dichiarazione</label><div class="actions"><button>Registra attestazione</button><button class="secondary" type="button" onclick="this.closest('dialog').close()">Annulla</button></div></form></div>`; dialog.showModal(); }
async function confirmTest(kind) { const test = state.tests[kind]; if (!test) return; test.passed = true; test.confirmedAt = isoNow(); const session = await selectedSession(); session.preflight = { ...(session.preflight || {}), [kind]: { passed: true, confirmedAt: test.confirmedAt } }; session.updatedAt = isoNow(); await state.store.put("sessions", session); renderCapture(); }

async function acquireDisplay() {
  if (state.capturing) return showNotice("Non puoi cambiare flussi durante una cattura. Metti in pausa il tratto prima.", "warn");
  stopStream(state.display); clearTest("system");
  try { state.display = await navigator.mediaDevices.getDisplayMedia({ video: { displaySurface: "monitor" }, audio: true, systemAudio: "include", preferCurrentTab: false }); const video = state.display.getVideoTracks()[0]; state.displayInfo = { surface: video?.getSettings().displaySurface || "sconosciuto", video: video?.readyState, audioTracks: state.display.getAudioTracks().length }; state.display.getTracks().forEach((track) => track.addEventListener("ended", () => handleTrackEnded("flusso monitor/audio terminato"), { once: true })); if (state.displayInfo.surface !== "monitor") showNotice("È stata scelta una superficie diversa dal monitor: non è modalità completa.", "warn"); }
  catch (error) { showNotice(`Cattura monitor/audio non disponibile: ${error.name}. ${error.message || ""}`, "danger"); state.display = null; state.displayInfo = null; }
  renderCapture();
}
async function acquireMic() { if (state.capturing) return showNotice("Non puoi cambiare flussi durante una cattura. Metti in pausa il tratto prima.", "warn"); stopStream(state.mic); clearTest("mic"); try { state.mic = await navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: false, noiseSuppression: false } }); state.mic.getTracks().forEach((track) => track.addEventListener("ended", () => handleTrackEnded("microfono terminato"), { once: true })); } catch (error) { showNotice(`Microfono non disponibile: ${error.name}. ${error.message || ""}`, "danger"); state.mic = null; } renderCapture(); }
function stopStream(stream) { stream?.getTracks().forEach((track) => track.stop()); }
function clearTest(kind) { if (state.tests[kind]?.url) URL.revokeObjectURL(state.tests[kind].url); state.tests[kind] = null; }
async function handleTrackEnded(cause) { if (state.capturing && !state.stopping) await stopCapture("interrotta", cause); }

async function sample(kind) {
  const track = kind === "system" ? state.display?.getAudioTracks()[0] : state.mic?.getAudioTracks()[0]; if (!track) return showNotice("Traccia audio non presente.", "danger");
  let recorder; try { recorder = new MediaRecorder(new MediaStream([track])); } catch (error) { return showNotice(`Campione non disponibile: ${error.message}`, "danger"); }
  const parts = []; recorder.ondataavailable = (event) => event.data.size && parts.push(event.data);
  recorder.onstop = () => { const blob = new Blob(parts, { type: recorder.mimeType }); state.tests[kind] = { url: URL.createObjectURL(blob), passed: false, createdAt: isoNow() }; renderCapture(); };
  recorder.start(); window.setTimeout(() => recorder.state !== "inactive" && recorder.stop(), 3_000); showNotice(`Campionamento ${kind === "system" ? "dell’audio computer" : "del microfono"} in corso: usa ora il segnale innocuo.`);
}
function playTest(kind) { const test = state.tests[kind]; if (!test?.url) return; dialog.innerHTML = `<div class="dialog-body"><h2>Riascolto separato: ${kind === "system" ? "audio computer" : "microfono"}</h2><audio controls autoplay src="${test.url}"></audio><p class="small">Segna “riconoscibile” solo se il contenuto atteso si sente in questa traccia, non nell’altra.</p><button class="secondary" onclick="this.closest('dialog').close()">Chiudi</button></div>`; dialog.showModal(); }
function refreshMeters() { for (const [key, teardown] of state.meters) teardown(); state.meters.clear(); for (const [kind, stream] of [["request-display", state.display], ["request-mic", state.mic]]) { const track = kind === "request-display" ? stream?.getAudioTracks()[0] : stream?.getAudioTracks()[0]; const el = byId(`meter-${kind}`); if (!track || !el) continue; const ctx = new AudioContext(), analyser = ctx.createAnalyser(), source = ctx.createMediaStreamSource(new MediaStream([track])); analyser.fftSize = 256; source.connect(analyser); const values = new Uint8Array(analyser.frequencyBinCount); let frame; const tick = () => { analyser.getByteTimeDomainData(values); let total = 0; values.forEach((value) => total += Math.abs(value - 128)); el.style.width = `${Math.min(100, total / values.length * 2.2)}%`; frame = requestAnimationFrame(tick); }; tick(); state.meters.set(kind, () => { cancelAnimationFrame(frame); source.disconnect(); ctx.close(); }); } }

class Segmenter {
  constructor(kind, stream, recording, session) { this.kind = kind; this.stream = stream; this.recording = recording; this.session = session; this.active = false; this.recorder = null; this.timer = null; this.sequence = 0; this.settled = Promise.resolve(); }
  start() { this.active = true; this.begin(); }
  begin() { if (!this.active || !state.capturing) return; const startedAt = Date.now(), parts = []; let settle; this.settled = new Promise((resolve) => { settle = resolve; }); try { this.recorder = new MediaRecorder(this.stream); } catch (error) { settle(); failCapture(`MediaRecorder ${this.kind}: ${error.message}`); return; } const recorder = this.recorder; recorder.ondataavailable = (event) => event.data.size && parts.push(event.data); recorder.onerror = (event) => { failCapture(`MediaRecorder ${this.kind}: ${event.error?.name || "errore"} ${event.error?.message || ""}`); }; recorder.onstop = async () => { try { const endedAt = Date.now(), blob = new Blob(parts, { type: recorder.mimeType }); if (blob.size) await saveBlock(this, blob, startedAt, endedAt); else await failCapture(`MediaRecorder ${this.kind}: blocco vuoto`); } finally { settle(); if (this.active && state.capturing) this.begin(); } }; recorder.start(); this.timer = window.setTimeout(() => this.stop(), CHUNK_MS); }
  stop() { window.clearTimeout(this.timer); if (this.recorder?.state !== "inactive") this.recorder.stop(); }
  async finish() { this.active = false; this.stop(); await this.settled; }
}
async function saveBlock(segmenter, blob, startedAt, endedAt) {
  const item = { id: id("block"), recordingId: segmenter.recording.id, sessionId: segmenter.session.id, index: segmenter.sequence++, stream: segmenter.kind, startMs: sessionOffset(segmenter.session.startedAt, startedAt), endMs: sessionOffset(segmenter.session.startedAt, endedAt), format: blob.type || "video/webm", bytes: blob.size, status: "scritto", verifiedAt: null, blob, createdAt: isoNow() };
  try { await assertStorage(blob.size); await state.store.put("chunks", item); const playable = await isPlayable(blob, segmenter.kind); item.status = playable ? "confermato" : "non verificabile"; item.verifiedAt = isoNow(); await state.store.put("chunks", item); await updateStorage(); if (state.view === "capture") renderCapture(); }
  catch (error) { await failCapture(`scrittura o quota: ${error.name || "errore"} ${error.message || ""}`); }
}
async function assertStorage(nextBytes) { const estimate = await navigator.storage?.estimate?.(), admission = storageAdmission(estimate, nextBytes); if (!admission.allowed) throw new DOMException("Quota locale quasi esaurita", "QuotaExceededError"); return admission; }
function isPlayable(blob, kind) { return new Promise((resolve) => { const media = document.createElement(kind === "display" ? "video" : "audio"), url = URL.createObjectURL(blob); let done = false; const finish = (value) => { if (done) return; done = true; URL.revokeObjectURL(url); resolve(value); }; media.onloadedmetadata = () => finish(Number.isFinite(media.duration)); media.onerror = () => finish(false); media.src = url; window.setTimeout(() => finish(false), 5_000); }); }
async function startCapture() {
  const session = await selectedSession(); const complete = captureIsLive({ displaySurface: state.displayInfo?.surface, displayTracks: state.display?.getTracks().map((track) => track.readyState) || [], microphoneTracks: state.mic?.getTracks().map((track) => track.readyState) || [], systemTest: state.tests.system, microphoneTest: state.tests.mic }); const reduced = byId("reduced-optin")?.checked;
  if (!complete && !reduced) return showNotice("Mancano le prove della modalità completa. Se scegli consapevolmente una modalità ridotta, attiva l’opt-in esplicito.", "warn");
  if (!state.display || !state.mic) return showNotice("Sono necessari monitor e microfono; selezionali prima.", "danger");
  let storagePreflight; try { storagePreflight = await assertStorage(0); } catch (error) { return showNotice(`Cattura non avviata: ${error.message}. Libera spazio o esporta i dati confermati.`, "danger"); }
  const recording = { ...nextRecording(session, complete ? "completa verificata" : "ridotta con opt-in"), storagePreflight: { ...storagePreflight, checkedAt: isoNow() } }; state.recording = recording; state.capturing = true; await state.store.put("recordings", recording); state.segmenters = [new Segmenter("display", state.display, recording, session), new Segmenter("microfono", state.mic, recording, session)]; state.segmenters.forEach((segmenter) => segmenter.start()); renderCapture();
}
async function stopCapture(status, cause) { if (!state.recording || state.stopping) return; state.stopping = true; state.capturing = false; const recording = state.recording, session = await state.store.get("sessions", recording.sessionId); await Promise.all(state.segmenters.map((segmenter) => segmenter.finish())); const closed = closeRecording(recording, session, status, cause); await state.store.put("recordings", closed); if (status === "interrotta") { const chunks = await state.store.byRecording("chunks", recording.id), gap = gapAfterConfirmed(closed, chunks, cause); if (gap) await state.store.put("gaps", { id: id("gap"), ...gap, createdAt: isoNow() }); } session.state = status === "interrotta" ? "interrotta/in attesa di scelta" : "aperta"; session.updatedAt = isoNow(); await state.store.put("sessions", session); state.recording = null; state.segmenters = []; stopStream(state.display); stopStream(state.mic); state.display = state.mic = null; state.displayInfo = null; state.tests = { system: null, mic: null }; state.stopping = false; await updateStorage(); renderCapture(); }
async function failCapture(cause) { if (state.recording) await stopCapture("interrotta", cause); else showNotice(cause, "danger"); }
async function concludeSession() { const session = await selectedSession(); if (state.recording) await stopCapture("terminata", "sessione conclusa"); const latest = await selectedSession(); latest.state = "conclusa"; latest.endedAt = isoNow(); latest.updatedAt = isoNow(); await state.store.put("sessions", latest); renderCapture(); }

async function runSearch(data) { const [sessions, notes, events, transcripts] = await Promise.all([state.store.all("sessions"), state.store.all("notes"), state.store.all("events"), state.store.all("transcripts")]); const from = data.get("from") ? new Date(data.get("from")).getTime() : 0, to = data.get("to") ? new Date(`${data.get("to")}T23:59:59`).getTime() : Infinity, sessionId = data.get("sessionId"); const visible = sessions.filter((session) => { const time = new Date(session.startedAt).getTime(); return time >= from && time <= to && (!sessionId || session.id === sessionId); }); const hits = searchDocuments(data.get("query"), { sessions: visible, notes, events, transcripts }, { includeOriginal: data.get("originalAsr") === "on" }).filter((hit) => visible.some((session) => session.id === hit.sessionId)); const target = byId("results"); target.innerHTML = hits.length ? hits.map((hit) => `<article class="result"><span class="chip">${esc(hit.kind)}</span><span class="small muted">${esc(hit.textStatus || "")}${hit.provenance ? ` · ${esc([hit.provenance.source, hit.provenance.engine, hit.provenance.version ? `v${hit.provenance.version}` : ""].filter(Boolean).join(" · "))}` : ""}</span><p>${esc(hit.text)}</p><button class="secondary compact" data-action="open-session" data-id="${hit.sessionId}" data-offset="${hit.offsetMs ?? ""}">${hit.offsetMs != null ? `Apri a ${formatTime(hit.offsetMs)}` : "Apri sessione"}</button></article>`).join("") : `<p class="muted">Nessun risultato nei documenti selezionati.</p>`; }
async function exportSession() { return exportScope(); }
async function exportScope(recordingId = null) { const session = await selectedSession(), stem = `${safeFileName(session.title)}${recordingId ? `-${recordingId.slice(-6)}` : ""}`, suggested = `${stem}-export.zip`; const [allRecordings, chunks, notes, events, gaps, allTranscripts] = await Promise.all([state.store.bySession("recordings", session.id), state.store.all("chunks"), state.store.bySession("notes", session.id), state.store.bySession("events", session.id), state.store.bySession("gaps", session.id), state.store.bySession("transcripts", session.id)]); const recordings = recordingId ? allRecordings.filter((item) => item.id === recordingId) : allRecordings; if (recordingId && recordings.length !== 1) return showNotice("Export del tratto non disponibile: il tratto non è più presente localmente.", "danger"); const scope = recordingId ? recordingExportScope(recordings[0], sessionOffset(session.startedAt)) : null, intervalStart = scope?.startMs ?? -Infinity, intervalEnd = scope?.endMs ?? Infinity, inScope = (item) => !recordingId || overlapsScope(item, intervalStart, intervalEnd); const allRelevant = chunks.filter((chunk) => chunk.sessionId === session.id && (!recordingId || chunk.recordingId === recordingId)).sort((a, b) => a.startMs - b.startMs), media = exportableChunks(allRelevant), chunksById = new Map(allRelevant.map((chunk) => [chunk.id, chunk])), transcripts = allTranscripts.filter((item) => inScope(item) && item.active !== false).map((item) => exportTranscript(item, chunksById)), relevantGaps = gaps.filter(inScope), scopedNotes = notes.filter(inScope), scopedEvents = events.filter(inScope), streams = Object.groupBy(allRelevant, (chunk) => chunk.stream); const manifest = { schemaVersion: 2, exportedAt: isoNow(), scope: { kind: recordingId ? "singolo tratto" : "sessione integrale", sessionId: session.id, parentSession: recordingId ? { id: session.id, title: session.title } : null, recordingId, interval: scope }, session, recordings, streams: Object.fromEntries(Object.entries(streams).map(([stream, blocks]) => [stream, { confirmedBlocks: blocks.filter((block) => block.status === "confermato").length, statuses: blocks.map((block) => ({ id: block.id, status: block.status, startMs: block.startMs, endMs: block.endMs, format: block.format })) }])), notes: scopedNotes, events: scopedEvents, gaps: relevantGaps, transcription: { status: session.asrStatus || "in attesa", coverage: session.asrCoverage || null, textSelection: "versione attiva", segments: transcripts }, chunks: allRelevant.map(({ blob, ...metadata }) => ({ ...metadata, file: metadata.status === "confermato" ? mediaFileName(metadata) : null })), notices: ["Solo i blocchi con stato confermato sono inclusi come media e dichiarati riproducibili.", "Il testo ordinario usa la versione attiva; ogni segmento dichiara provenienza e verificabilità della media sorgente.", "Lacune, blocchi non confermati e trascrizione incompleta restano nel manifest."] }; const entries = [{ name: "manifest.json", data: JSON.stringify(manifest, null, 2) }, { name: "trascrizione.txt", data: transcriptText(transcripts) }, ...media.map((chunk) => ({ name: mediaFileName(chunk), data: chunk.blob }))], size = media.reduce((total, chunk) => total + chunk.bytes, 0); const picker = window.showSaveFilePicker?.({ suggestedName: suggested, types: [{ description: "Archivio Diario", accept: { "application/zip": [".zip"] } }] }); let writable = null; if (picker) { try { writable = await (await picker).createWritable(); } catch (error) { if (error.name === "AbortError") return showNotice("Export annullato: nessun file creato."); showNotice(`Destinazione streaming non disponibile (${error.name}); preparo un archivio in memoria.`, "warn"); } } try { if (writable) await streamZip(entries, writable); else { if (size > 300 * 1024 * 1024) throw new Error("Archivio oltre 300 MB: usa Chrome con il salvataggio streaming disponibile e scegli una destinazione locale."); download(await createZip(entries), suggested); } showNotice(`Export concluso: ZIP con manifest, testo e ${media.length} blocchi confermati. La chiusura ZIP è riuscita; riaprilo sul target per la prova richiesta.`, "warn"); } catch (error) { try { await writable?.abort(); } catch {} showNotice(`Export non concluso: ${error.message}. I dati locali restano disponibili.`, "danger"); } }
async function playBlock(chunkId) { const chunk = await state.store.get("chunks", chunkId); if (!chunk) return showNotice("Blocco non più presente nell’archivio.", "danger"); if (chunk.status !== "confermato") return showNotice("Il blocco non è confermato e non viene usato per riproduzione affidabile.", "warn"); const url = URL.createObjectURL(chunk.blob), tag = chunk.stream === "display" ? "video" : "audio", offset = state.jumpOffset == null ? 0 : Math.max(0, (state.jumpOffset - chunk.startMs) / 1000); dialog.innerHTML = `<div class="dialog-body"><h2>Blocco ${esc(chunk.stream)} · ${formatTime(chunk.startMs)}</h2><${tag} id="playback-media" controls src="${url}"></${tag}><p class="small">Stato: <strong>${esc(chunk.status)}</strong>. ${offset ? `Avvio richiesto a ${formatTime(state.jumpOffset)}.` : ""} Il riascolto non colma intervalli mancanti.</p><button class="secondary" onclick="this.closest('dialog').close()">Chiudi</button></div>`; const media = dialog.querySelector("#playback-media"); media.addEventListener("loadedmetadata", () => { if (Number.isFinite(media.duration)) media.currentTime = Math.min(offset, Math.max(0, media.duration - .05)); media.play().catch(() => {}); }, { once: true }); dialog.addEventListener("close", () => URL.revokeObjectURL(url), { once: true }); dialog.showModal(); }
function download(blob, name) { const url = URL.createObjectURL(blob), link = document.createElement("a"); link.href = url; link.download = name; link.click(); window.setTimeout(() => URL.revokeObjectURL(url), 10_000); }
async function confirmDelete() { const session = await selectedSession(); dialog.innerHTML = `<div class="dialog-body"><h2>Rimuovere “${esc(session.title)}”?</h2><p>Verranno rimossi sessione, tratti, blocchi, note, eventi, lacune, trascrizioni e indice controllati dall’app in questo browser. Gli export e i backup esterni non vengono trovati o eliminati.</p><form data-form="delete"><div class="actions"><button class="danger">Rimuovi dati locali</button><button type="button" class="secondary" onclick="this.closest('dialog').close()">Annulla</button></div></form></div>`; dialog.showModal(); }

async function setAsr(status, detail = "", extra = {}) { state.asr = { ...state.asr, ...extra, id: "asr-settings", status, detail, updatedAt: isoNow() }; await state.store.put("settings", state.asr); const session = await selectedSession(); const target = byId("asr-status"); if (target && session) target.innerHTML = renderAsrStatus(session); }
async function smokeTestAsr(pipeline) {
  const response = await fetch("test/fixtures/italian-synthetic.wav"); if (!response.ok) throw new Error(`campione sintetico non disponibile (${response.status})`);
  const samples = await decodeTo16k(await response.blob()), metrics = audioMetrics(samples); if (metrics.seconds < 1 || metrics.rms < .005) throw new Error(`campione sintetico non valido (${metrics.seconds.toFixed(2)} s, RMS ${metrics.rms.toFixed(4)})`);
  const result = await pipeline(samples, { language: "italian", task: "transcribe", return_timestamps: true, chunk_length_s: 10, stride_length_s: 0 }), output = asrResultShape(result), segments = asrChunks(result, { startMs: 0, endMs: Math.round(metrics.seconds * 1000) });
  if (!segments.length) throw new Error(`campione sintetico senza testo (output text=${output.textChars}, chunks=${output.chunks}, nonVuoti=${output.nonEmptyChunks}, timestamp=${output.timestampedChunks})`);
  return { at: isoNow(), output, segments: segments.length, seconds: metrics.seconds, rms: metrics.rms };
}
async function prepareAsr(cacheOnly = false) {
  const profile = selectedAsrProfile(), model = profile.model;
  if (state.asrPipeline && state.asr.model === model && state.asr.profileId === profile.id) return state.asrPipeline;
  state.asrPipeline = null;
  if (cacheOnly && navigator.onLine) throw new Error("Disconnetti la rete prima della verifica cache offline.");
  if (profile.requiresWebGPU && !webGpuAvailable()) throw new Error("WebGPU non disponibile per il profilo ASR selezionato. Scegli un profilo WASM.");
  await setAsr(cacheOnly ? "verifica cache in corso" : "download in corso", cacheOnly ? "Ricerca esclusivamente file in cache locale." : "Scaricamento esplicito di runtime e modello in corso.");
  try {
    const progress_callback = (progress) => { const text = progress.status === "progress" && progress.total ? `${progress.file || "modello"}: ${Math.round(progress.progress || 0)}%` : progress.status || "preparazione"; const target = byId("asr-status"); if (target) target.textContent = `ASR locale: ${text}`; };
    state.asrPipeline = await createAsrPipeline({ profile, cacheOnly, progress: progress_callback });
    await setAsr("prova sintetica", "Verifico localmente che il profilo produca testo prima di dichiararlo pronto."); const smokeTest = await smokeTestAsr(state.asrPipeline);
    await setAsr("pronto", cacheOnly ? "Cache offline e campione sintetico verificati in questa sessione." : `${profile.label} pronto dopo il campione sintetico locale; nessun audio è stato inviato.`, { model, profileId: profile.id, dtype: profile.dtype, device: profile.device, runtime: "Transformers.js 3.8.1", provider: "jsDelivr + Hugging Face", smokeTest, downloadedAt: state.asr.downloadedAt || isoNow(), cacheVerifiedAt: cacheOnly ? isoNow() : state.asr.cacheVerifiedAt || null }); await updateStorage();
    return state.asrPipeline;
  } catch (error) { state.asrPipeline = null; await setAsr("errore", `${error.name || "Errore"}: ${error.message || "preparazione ASR non riuscita"}`); throw error; }
}
async function verifyAsrOffline() { try { state.asrPipeline = null; await prepareAsr(true); showNotice("Cache ASR verificata senza rete in questa sessione."); } catch (error) { showNotice(error.message, "warn"); } }
async function transcribeSession() {
  const session = await selectedSession(), source = byId("asr-source")?.value || "microfono", profile = selectedAsrProfile(), model = profile.model; if (!session) return;
  if (!canTranscribe({ pipelineReady: !!state.asrPipeline, preparedModel: state.asr.model, selectedModel: model })) return showNotice("Prepara esplicitamente il modello selezionato prima di trascrivere. Nessun download è avviato da questo comando.", "warn");
  try {
    const pipeline = state.asrPipeline; const chunks = exportableChunks((await state.store.all("chunks")).filter((item) => item.sessionId === session.id && item.stream === source));
    if (!chunks.length) return showNotice(`Non ci sono blocchi ${source} confermati da trascrivere.`, "warn");
    session.asrStatus = "in elaborazione"; session.updatedAt = isoNow(); await state.store.put("sessions", session); await setAsr("in elaborazione", `${chunks.length} blocchi ${source} in coda.`);
    const runId = id("asr-run"), failures = [], diagnostics = [], successfulBlockIds = new Set(), prior = await state.store.bySession("transcripts", session.id);
    for (let index = 0; index < chunks.length; index += 1) { const block = chunks[index], diagnostic = { blockId: block.id, startMs: block.startMs, format: block.format, model, profileId: profile.id, dtype: profile.dtype, device: profile.device, startedAt: isoNow() }; diagnostics.push(diagnostic); const started = performance.now(); await setAsr("in elaborazione", `Blocco ${index + 1}/${chunks.length} · ${formatTime(block.startMs)}.`); try { const audio = await decodeTo16k(block.blob), metrics = audioMetrics(audio); Object.assign(diagnostic, metrics); if (metrics.seconds < .2 || metrics.rms < .005) throw new Error(`segnale insufficiente: ${metrics.seconds.toFixed(2)} s, RMS ${metrics.rms.toFixed(4)}`); const result = await pipeline(audio, { language: "italian", task: "transcribe", return_timestamps: true, chunk_length_s: 10, stride_length_s: 0 }); const output = asrResultShape(result), timestampStrategy = usesWholeBlockTimestamp(result) ? "intera-durata-blocco; output-senza-chunk" : "timestamp-del-modello"; Object.assign(diagnostic, { output, timestampStrategy }); const recognized = asrChunks(result, block); if (!recognized.length) throw new Error(`ASR senza testo/segmenti nonostante segnale valido (output text=${output.textChars}, chunks=${output.chunks}, nonVuoti=${output.nonEmptyChunks}, timestamp=${output.timestampedChunks})`); for (const segment of recognized) await state.store.put("transcripts", { id: id("asr"), sessionId: session.id, recordingId: block.recordingId, blockId: block.id, ...segment, source: "asr-locale", asrSource: source, engine: "Transformers.js 3.8.1", model, profileId: profile.id, dtype: profile.dtype, device: profile.device, timestampStrategy, runId, version: 1, active: true, createdAt: isoNow() }); successfulBlockIds.add(block.id); } catch (error) { diagnostic.error = error.message || error.name || "errore"; failures.push(`${formatTime(block.startMs)}: ${diagnostic.error}`); } finally { diagnostic.elapsedMs = performance.now() - started; } }
    const superseded = supersededAsrSegmentsForBlocks(prior, source, successfulBlockIds); for (const item of superseded) { item.active = false; await state.store.put("transcripts", item); }
    const allFailed = failures.length === chunks.length; session.asrStatus = allFailed ? "errore" : failures.length ? "parziale" : "completa"; session.asrCoverage = { ...(session.asrCoverage || {}), [source]: { blocks: chunks.length, failed: failures.length, diagnostics, runId, updatedAt: isoNow() } }; session.updatedAt = isoNow(); await state.store.put("sessions", session); await setAsr(allFailed ? "errore" : "pronto", allFailed ? `ASR non ha prodotto testo nei ${chunks.length} blocchi con segnale valido; audio e precedenti trascrizioni restano conservati. ${failures.join(", ")}` : failures.length ? `Trascrizione parziale; audio e precedenti trascrizioni dei blocchi falliti sono conservati. ${failures.join(", ")}` : `Trascrizione completa di ${chunks.length} blocchi ${source}.`); renderCapture();
  } catch (error) { session.asrStatus = "errore"; session.updatedAt = isoNow(); await state.store.put("sessions", session); await setAsr("errore", `${error.name || "Errore"}: ${error.message || "ASR fallito; audio conservato."}`); renderCapture(); }
}

init();
